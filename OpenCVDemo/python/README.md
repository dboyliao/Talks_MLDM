# Python Trainer for a Simple Neural Network

run `make train` to train the model.

you will see a `model.txt` file which is ready to be loaded in `OpenCVDemo` project after the training.

run `make clean` to clean up.
